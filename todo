TODO

  Media
    Update media, including favorite or explicit
    Search for media by title, subtitle, tags, date range, limit, offset, experience OR drug

  twilio endpoints
    add note
    images
      add image
      rename image
      tag image
    consumptions
      list current
      set count
      duplicate to now
      jump to now

  add other owner check to delete; update drug, method, experience, consumption
